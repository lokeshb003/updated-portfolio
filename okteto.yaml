name: react-app

build:
  frontend:
    context: .
    dockerfile: Dockerfile

deploy:
  - helm upgrade --install my-react-app chart --set frontend.image=${OKTETO_BUILD_FRONTEND_IMAGE}

dev:
  frontend:
    command: ["npm", "start"]
    sync:
      - .:/root
    forward:
      - 3000:3000
